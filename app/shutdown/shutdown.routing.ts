import { RouterConfig }         from '@angular/router';
import { ShutdownComponent }    from './shutdown.component';

export const shutdownRoutes: RouterConfig = [
  { path: 'shutdown', component: ShutdownComponent }
];
