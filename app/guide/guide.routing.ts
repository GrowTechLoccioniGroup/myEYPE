import { RouterConfig }         from '@angular/router';
import { GuideComponent }    from './guide.component';

export const guideRoutes: RouterConfig = [
  { path: 'guide', component: GuideComponent }
];
