import { RouterConfig }         from '@angular/router';
import { SiteComponent }    from './site.component';

export const siteRoutes: RouterConfig = [
  { path: 'website', component: SiteComponent }
];
